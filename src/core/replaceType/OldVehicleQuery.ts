/**
 * reemplazar la query OldVehicleQuery por NewVehicleQuery, el refactor está a medio
 * hacer, la nueva query se usa en vehicle repository per la old query se sigue
 * usando en el marcher y en los tests
 */

export class OldVehicleQuery {
	public brandDescription: string;
}
